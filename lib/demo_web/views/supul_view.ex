defmodule DemoWeb.SupulView do
  use DemoWeb, :view

  # def nation_select_options(nation_supuls) do
  #   for nation_supul <- nation_supuls, do: {nation_supul.name, nation_supul.id}
  # end

  # def state_select_options(state_supuls) do
  #   for state_supul <- state_supuls, do: {state_supul.name, state_supul.id}
  # end
end
