<h1>Edit Fiat pool</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.fiat_pool_path(@conn, :update, @fiat_pool)) %>

<span><%= link "Back", to: Routes.fiat_pool_path(@conn, :index) %></span>
