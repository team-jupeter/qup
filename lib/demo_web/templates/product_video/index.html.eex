<h3>Listing Videos</h3>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for video <- @videos do %>
    <tr>
      <td><%= video.title %></td>
      <td><%= link "Edit", to: Routes.product_video_path(@conn, :edit, video) %></td>
      <td>
        <%= link "Delete", to: Routes.product_video_path(@conn, :delete, video),
                           method: :delete,
                           data: [confirm: "Are you sure?"] %>
      </td>
      <td>
        <%= link "Watch", to: Routes.product_watch_path(@conn, :show, video),
                          class: "button" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Video", to: Routes.product_video_path(@conn, :new) %></span>
